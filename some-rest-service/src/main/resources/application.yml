spring:
  application:
    name:
      greetings-service

info:
  version: 0.0.1

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/